# Used for metadata (Open Graph, canonical URLs). No trailing slash.
# Omit or leave empty for local dev — the app defaults to http://localhost:3000
#
# Production (set in Vercel / your host):
# NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app
NEXT_PUBLIC_SITE_URL=
